#include <stdio.h>

unsigned long long factorial(int n) {
    unsigned long long result = 1;
    for (int i = 1; i <= n; i++) {
        result *= i;
    }
    return result;
}

int main() {
    int n = 25;
    unsigned long long result = factorial(n);
    printf("25! = %llu\n", result);
    return 0;
}
